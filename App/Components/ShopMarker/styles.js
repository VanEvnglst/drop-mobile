import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  callOutContainer: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  callOutTitle: {
    fontSize: 15,
    textAlign: 'center',
  },
  callOutDescription: {
    fontSize: 13,
    textAlign: 'center',
  },
})
